# Database connection URL
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/sportmap_db

# Mapbox Token for Geocoding (Optional, uses MOCK_TOKEN if missing)
MAPBOX_TOKEN=your_mapbox_token_here
